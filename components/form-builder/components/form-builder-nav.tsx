import { Button } from "../../ui/button";

export function FormBuilderNav() {
  return (
    <nav className="h-full">
      <section className="h-full flex justify-end items-center">
        <Button type="submit">Publicar</Button>
      </section>
    </nav>
  );
}
