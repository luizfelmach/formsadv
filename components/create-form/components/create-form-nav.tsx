import { Button } from "../../ui/button";

export function CreateFormNav() {
  return (
    <nav className="h-full">
      <section className="h-full flex justify-end items-center">
        <Button>Publicar</Button>
      </section>
    </nav>
  );
}
